<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h4>Update product</h4>
    <form method="POST"
    action="/v1/products/<%= product.id %>?_method=patch">
        <div>
            <label for="name">Product Name</label>
            <input type="text" name="name"  value="<%= product.name %>" placeholder="Enter Your Product's name"></input>
        </div>
        <div>
            <label for="desc">Description</label>
            <input type="text" name="desc" value="<%= product.desc %>" placeholder="Enter Your Product's Description"></input>
        </div>
        <div>
            <label for="price">Price</label>
            <input type="number" name="price" value="<%= product.price %>" placeholder="Enter Your Product's Price (in INR)"></input>
        </div>
        <div>
            <label for="qty">Quantity</label>
            <input type="number" name="qty" value="<%= product.qty %>" placeholder="Enter Your Product's Quantity"></input>
        </div>
        <div>
            <label for="category">category</label>
            <select name="category" id="">
                <option value="Apparel" <%= product.category === "Apparel" ? "selected": "" %>>Apparel</option>
                <option value="Electronics" <%= product.category === "Electronics" ? "selected": "" %> >Electronics</option>
                <option value="Cosmetics" <%= product.category === "Cosmetics" ?"selected": "" %> >Cosmetics</option>
            </select>
            <div>
                <label for="image">Upload Image</label>
                <input type="file" name="image" placeholder="Upload your product's image"></input>
            </div>
        </div>
        <button type="submit">Update Product</button>
    </form>
</body>
</html>